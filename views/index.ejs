<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CLOUD CRASH Portal</title>
    <link rel="icon" type="image/x-icon" href="../assets/favicon.png">

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Exo+2:wght@300;400;500;600;700&family=Quantico:wght@400;700&display=swap" rel="stylesheet">

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        :root {
            --primary: #000000;
            --accent: #007bff;
            --accent-alt: #00c4ff;
            --glow: 0 0 15px rgba(0, 123, 255, 0.8), 0 0 30px rgba(0, 196, 255, 0.5);
            --text-primary: #ffffff;
            --text-secondary: #b8d4ff;
            --transition: all 0.3s cubic-bezier(0.4, 0.0, 0.2, 1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Exo 2', sans-serif;
            background: var(--primary);
            color: var(--text-primary);
            min-height: 100vh;
            overflow: hidden;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 20px;
            position: relative;
        }

        /* Particle Canvas */
        #particle-canvas {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
        }

        /* Header */
        .header {
            text-align: center;
            margin-bottom: 50px;
        }

        .logo {
            width: 90px;
            height: 90px;
            margin: 0 auto 20px;
            background: linear-gradient(135deg, var(--accent), var(--accent-alt));
            border-radius: 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: var(--glow);
            border: 2px solid var(--accent);
            animation: logoPulse 2.8s ease-in-out infinite;
        }

        @keyframes logoPulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.07); }
        }

        .logo i {
            font-size: 36px;
            color: var(--text-primary);
        }

        .title {
            font-family: 'Quantico', sans-serif;
            font-size: 44px;
            font-weight: 700;
            margin-bottom: 15px;
            letter-spacing: 4px;
            background: linear-gradient(to right, var(--accent), var(--accent-alt));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            position: relative;
            animation: glitchShift 4s linear infinite;
            text-transform: uppercase;
        }

        @keyframes glitchShift {
            0% { transform: translate(0, 0); }
            5% { transform: translate(2px, -2px) skew(2deg); }
            10% { transform: translate(-2px, 2px) skew(-2deg); }
            15%, 100% { transform: translate(0, 0); }
        }

        .title::before,
        .title::after {
            content: 'CLOUD CRASH';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(to right, var(--accent), var(--accent-alt));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            opacity: 0.25;
        }

        .title::before {
            animation: glitchTop 3.2s linear infinite;
            clip-path: polygon(0 0, 100% 0, 100% 35%, 0 35%);
        }

        .title::after {
            animation: glitchBottom 3.7s linear infinite;
            clip-path: polygon(0 65%, 100% 65%, 100% 100%, 0 100%);
        }

        @keyframes glitchTop {
            0% { transform: translate(0, 0); }
            5% { transform: translate(3px, -3px); }
            10% { transform: translate(-3px, 3px); }
            15%, 100% { transform: translate(0, 0); }
        }

        @keyframes glitchBottom {
            0% { transform: translate(0, 0); }
            5% { transform: translate(-3px, 3px); }
            10% { transform: translate(3px, -3px); }
            15%, 100% { transform: translate(0, 0); }
        }

        .subtitle {
            color: var(--text-secondary);
            font-size: 20px;
            font-weight: 400;
            opacity: 0.9;
        }

        /* Sign-In Button */
        .login-button {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 100%;
            max-width: 380px;
            padding: 18px 28px;
            background: linear-gradient(135deg, var(--accent) 0%, var(--accent-alt) 100%);
            color: var(--text-primary);
            border: 2px solid var(--accent);
            border-radius: 12px;
            font-family: 'Quantico', sans-serif;
            font-size: 18px;
            font-weight: 700;
            letter-spacing: 2px;
            text-transform: uppercase;
            cursor: pointer;
            transition: var(--transition);
            box-shadow: var(--glow);
            position: relative;
            overflow: hidden;
            text-decoration: none;
        }

        .login-button::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(
                90deg,
                transparent,
                rgba(255, 255, 255, 0.5),
                transparent
            );
            transition: 0.4s;
        }

        .login-button:hover {
            transform: scale(1.06);
            box-shadow: 0 12px 40px rgba(0, 123, 255, 0.9);
            border-color: var(--accent-alt);
        }

        .login-button:hover::before {
            left: 100%;
        }

        .login-button i {
            margin-right: 12px;
            font-size: 24px;
        }

        /* Footer */
        .footer {
            text-align: center;
            margin-top: 40px;
            color: var(--text-secondary);
            font-size: 15px;
            opacity: 0.8;
        }

        /* Responsive Adjustments */
        @media (max-width: 576px) {
            .title {
                font-size: 34px;
                letter-spacing: 2px;
            }

            .subtitle {
                font-size: 16px;
            }

            .login-button {
                font-size: 16px;
                padding: 14px 20px;
                max-width: 280px;
            }

            .logo {
                width: 70px;
                height: 70px;
            }

            .logo i {
                font-size: 28px;
            }
        }
    </style>
</head>
<body>
    <!-- Particle Canvas -->
    <canvas id="particle-canvas"></canvas>

    <!-- Main Content -->
    <div class="header">
        <div class="logo">
            <i class="fas fa-cloud"></i>
        </div>
        <h1 class="title">CLOUD CRASH</h1>
        <p class="subtitle">BlueCore Server Network</p>
    </div>

    <a href="../login" class="login-button">
        <i class="fab fa-discord"></i>
        SIGN IN WITH DISCORD
    </a>

    <div class="footer">
        <p>© 2025 CLOUDCRASH Systems. All systems online.</p>
    </div>

    <script>
        // White Floating Particles
        document.addEventListener('DOMContentLoaded', function() {
            const canvas = document.getElementById('particle-canvas');
            const ctx = canvas.getContext('2d');
            let width = window.innerWidth;
            let height = window.innerHeight;

            canvas.width = width;
            canvas.height = height;

            const particles = [];
            const numParticles = 80;

            class Particle {
                constructor() {
                    this.x = Math.random() * width;
                    this.y = Math.random() * height;
                    this.velocity = {
                        x: (Math.random() - 0.5) * 0.8,
                        y: (Math.random() - 0.5) * 0.8
                    };
                    this.radius = Math.random() * 2 + 1;
                    this.opacity = Math.random() * 0.5 + 0.3;
                }

                update() {
                    this.x += this.velocity.x;
                    this.y += this.velocity.y;

                    // Wrap around edges
                    if (this.x < 0) this.x += width;
                    if (this.x > width) this.x -= width;
                    if (this.y < 0) this.y += height;
                    if (this.y > height) this.y -= height;
                }

                draw() {
                    ctx.beginPath();
                    ctx.arc(this.x, this.y, this.radius, 0, Math.PI * 2);
                    ctx.fillStyle = `rgba(255, 255, 255, ${this.opacity})`;
                    ctx.shadowBlur = 10;
                    ctx.shadowColor = 'rgba(255, 255, 255, 0.5)';
                    ctx.fill();
                }
            }

            // Initialize particles
            for (let i = 0; i < numParticles; i++) {
                particles.push(new Particle());
            }

            // Animation loop
            function animate() {
                ctx.clearRect(0, 0, width, height);
                particles.forEach(particle => {
                    particle.update();
                    particle.draw();
                });
                requestAnimationFrame(animate);
            }

            // Resize canvas
            window.addEventListener('resize', () => {
                width = window.innerWidth;
                height = window.innerHeight;
                canvas.width = width;
                canvas.height = height;
            });

            animate();
        });
    </script>
</body>
</html>
